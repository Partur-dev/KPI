services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ipishnyk
      POSTGRES_PASSWORD: ipishnyky
      POSTGRES_DB: lab4-nodejs
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
